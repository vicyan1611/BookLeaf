import { ReactNode } from 'react'
import LoginPage from '../../components/LoginPage'

class LoginPageAdmin extends LoginPage {
    public constructor(props: any) {
        super(props)
    }
    public render(): ReactNode {
        return super.render()
    }
}

export default LoginPageAdmin;